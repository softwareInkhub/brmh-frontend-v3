// Catch-all route for settings dynamic URLs
// This handles URLs like /settings/api, /settings/security
// and redirects to the main settings page

import SettingsPage from '../page';

export default SettingsPage;

